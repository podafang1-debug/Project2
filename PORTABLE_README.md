# 启智训练台便携版使用说明

## 启动

1. 将整个文件夹完整解压到本地磁盘，不要直接在压缩包内运行。
2. 双击 `start-platform.bat`。
3. 保留弹出的命令窗口，浏览器会自动打开平台。
4. 首次注册康复专业人员时，按页面要求输入命令窗口显示的一次性安全初始化码。

平台自带 Python 运行时、OCR、CrewAI、Ollama 和 `qwen3:0.6b` 模型，不需要另外安装。AI 默认开启，只连接本机 `127.0.0.1`；本地模型启动失败时会自动使用规则方案，基本训练仍可继续。

## 数据

首次启动会在 `backend` 目录创建新的数据库、文档密钥和必要目录。请定期使用康复专业人员端的备份功能，并妥善保管整个便携版文件夹。

不要把已经录入真实儿童资料的运行目录直接转发给其他人。需要重新分发时，应从原项目运行 `打包便携版.bat` 生成新的空白交付包。

## 关闭

回到命令窗口按 `Ctrl+C` 停止平台；启动器会同时关闭它启动的本地 Ollama 进程。

