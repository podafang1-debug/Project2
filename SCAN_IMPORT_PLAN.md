# 扫描档案批量导入实施方案

## 目标

将批量扫描 PDF 转换为可追溯、经专业复核的儿童历史档案、评估工具记录和初始六维能力画像。系统不从模糊手写或叙述文本猜测诊断和分数。

## 完整流程

1. 康复医疗专业人员选择多份 PDF，或使用文件夹批量脚本。
2. 系统计算 SHA-256，跳过重复文件，并用本机 Fernet 密钥加密原件。
3. PyMuPDF 逐页渲染，RapidOCR 在本机识别中文，不调用网络接口。
4. 规则引擎将页面分为接案、评估、计划、训练记录、考勤、总结、协议和其他。
5. 系统提取基础档案、量表名称和风险词等候选字段，同时保存页码、证据片段和置信度。
6. 康复医疗专业人员逐字段修改、确认或排除。诊断、风险、评估信息永不自动通过。
7. 全部候选字段处理完成后，关联授权儿童并签署导入。
8. 已确认字段写入不可变接案记录；量表名称写入历史评估记录。
9. 只有专业人员完整填写六维分数时才生成初始画像；否则仅入档，等待合法评估补录。
10. 批次创建、字段审核和正式入档均写入审计日志。

## 权限边界

- 儿童：不能查看或操作扫描档案。
- 家长：不能上传、查看原件或确认临床字段。
- 康复医疗专业人员：上传、查看、复核、关联儿童、签署入档。
- 内容与机构管理员：仅查看脱敏批次数量和运行状态，不能查看文件名与临床内容。

## 数据落点

- 加密 PDF：`backend/private_documents/<batch_id>/`
- 本机密钥：`backend/.document_key`
- SQLite：批次、文档元数据、逐页 OCR 密文、候选字段、审核记录、审计日志。
- 正式业务表：`care_records`、`assessments`、`ability_profiles`。

密钥和加密档案不会进入 Git。生产部署应将密钥迁移到机构密钥管理系统，并对备份实施单独加密和恢复演练。

## 200 份材料运行建议

1. 先选择 10-20 份覆盖不同年份和模板的材料试运行。
2. 修订字段规则和页面分类关键词。
3. 按 20-50 份拆分批次，避免一次任务失败影响全部材料。
4. 完成每批复核后再开始下一批正式入档。
5. 对高风险字段、量表分数和随机样本进行二次复核。

## 验收标准

- 重复 PDF 不产生第二份原件。
- 原件和 OCR 全文不以明文落盘。
- 每个候选字段能追溯到文件、页码和证据片段。
- 未复核字段不能正式入档。
- 非专业角色调用导入接口返回 403。
- 六维分数不完整时不能生成画像。
- 原有登录、四角色、儿童基线游戏、专业闭环测试全部通过。
