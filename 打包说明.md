# 启智训练台便携版打包说明

双击 `打包便携版.bat` 即可开始。打包程序只支持 64 位 Windows 10/11，首次运行需要联网并预留足够磁盘空间；不要求提前安装 Python、CrewAI、OCR 或 Ollama。

程序会自动：

1. 下载官方 uv，并在项目的 `.portable-build` 中安装隔离的 Python 3.13。
2. 安装锁定版本的 PyInstaller、CrewAI 和 OCR/PDF 依赖。
3. 检查依赖中不得出现 LiteLLM。
4. 将 Python 后端打成独立 Windows 程序。
5. 下载官方 Ollama Windows 独立运行时和 `qwen3:0.6b` 模型。
6. 组装并检查交付包，明确排除数据库、账号、档案、密钥、备份和 Git 历史。
7. 生成文件 SHA-256 清单和可传输的 ZIP 压缩包。

默认输出：

- 文件夹：`release/QizhiTrainingPlatform-Portable/`
- 压缩包：`release/QizhiTrainingPlatform-Portable.zip`

如果只需要文件夹，可在 PowerShell 中运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\packaging\build_portable.ps1 -FolderOnly
```

重复打包时若希望复用已经下载的 Ollama 压缩包：

```powershell
powershell -ExecutionPolicy Bypass -File .\packaging\build_portable.ps1 -ReuseDownloads
```

打包程序不会启动最终平台进行冒烟测试。构建完成后请在一台没有开发环境的 Windows 电脑上自行双击 `start-platform.bat` 验收。

